apiVersion: v1
kind: Service
metadata:
  name: {{ include "zvezda.fullname" . }}
  labels:
    {{- include "zvezda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zvezda.selectorLabels" . | nindent 4 }}
